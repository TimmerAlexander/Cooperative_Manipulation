<?xml version="1.0"?>
<launch>

    <!-- <arg name="panda_name" default="panda"/>
    <arg name="base_name" default="$(arg panda_name)/base"/>
 
    <group ns="$(arg panda_name)"> -->

        <!-- Launch the franka_joint_torque_controller -->
  <rosparam command="load" file="$(find cooperative_manipulation_controllers)/config/franka_impedance_controller.yaml" />
  <node name="controller_spawner" pkg="controller_manager" type="spawner" respawn="false" output="screen"  args="franka_impedance_controller"/>

    <!-- </group>   -->

</launch>

